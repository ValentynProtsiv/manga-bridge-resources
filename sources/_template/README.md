# Resource template

Copy this folder to `sources/<resource-id>/` when preparing a new MangaBridge resource package.

The package contains metadata only. Parser and request implementation belongs in the MangaBridge app adapter until the resource runtime is split into separately distributed code.
