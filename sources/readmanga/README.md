# ReadManga

ReadManga resource package metadata for MangaBridge.

This package contains metadata only. Runtime parsing and guarded request logic live in the MangaBridge app adapter.

The resource supports configurable mirrors and optional resource-scoped login sessions controlled by the user in MangaBridge.
